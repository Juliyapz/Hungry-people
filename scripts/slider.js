const specSlider = new Swiper('.spec__slider', {
  loop: true,

  pagination: {
    el: '.spec__pagination',
    bulletClass: ('spec__bullet'),
    bulletActiveClass: ('spec__bullet--active'),
  },

  // autoplay: {
  //   delay: 4000
  // },

});
